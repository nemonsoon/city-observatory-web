# City Observatory Design System

[← README に戻る](README.md)

観測記録紙を下敷きにしたデザインシステム。明るい配色は方眼紙、暗い配色は青焼き図面とする。

このファイルが、配色・書体・余白・罫線・動きの唯一の正典。画面の見た目に関する判断はここに従う。

関連する正典は次のとおり。ここには複製しない。

- 画面に何を出すか: [要件定義書](docs/requirements.md)
- トークンを実装に落とす場所: [技術仕様書](docs/technical-specifications.md)
- コードの書き方: [コーディング規約](docs/coding-guidelines.md)

## 1. Visual Theme

City Observatory は都市の気象と大気を観測して記録するアプリケーションであり、画面は観測値を書き込む記録紙として振る舞う。

紙は方眼である。方眼の間隔は余白の単位と一致し、グラフの目盛りも同じ間隔で並ぶ。背景・余白・グラフが1つの格子を共有することが、この画面の視覚的な芯になる。

観測値は等幅書体で桁を揃えて置き、罫線で区切る。色は観測値そのものと、その段階を示すためだけに使う。面を塗る装飾、透過、影による浮遊表現は使わない。

**構成要素**

- 方眼を敷いた紙（明: 淡青灰の方眼紙 / 暗: 青焼き図面）。内容と一緒にスクロールする
- 罫線で区切られた観測欄
- 等幅書体で桁を揃えた観測値
- 段階を示す5段階の色と、それに必ず添える文字ラベル
- 読み込み時に罫線が左から引かれる1回の動き

**公共データ可視化の制約**

英国政府デザインシステムと英国国家統計局のデータ可視化指針から、次の3点を守る。

| 制約                         | 実装上の意味                                 |
| ---------------------------- | -------------------------------------------- |
| 色だけで情報を伝えない       | 段階色を使う箇所には必ず文字ラベルを併記する |
| 重要でない補足情報を載せない | 1つの欄は1つの観測値だけを持つ               |
| 目盛りはゼロから始める       | グラフの Y 軸は 0 を含む範囲で描く           |

## 2. Color

すべて OKLCH で定義し、`app/globals.css` の `:root` と `.dark` に置く。Tailwind からはセマンティックトークン名で参照する。

### Foundation

| Token              | Light                         | Dark                          | Role                             |
| ------------------ | ----------------------------- | ----------------------------- | -------------------------------- |
| `background`       | `oklch(0.9531 0.0035 219.53)` | `oklch(0.2103 0.0279 240.93)` | 紙の地。明は淡青灰、暗は青焼き   |
| `foreground`       | `oklch(0.2451 0.0241 241.35)` | `oklch(0.9211 0.0136 226.57)` | 主テキスト、観測値               |
| `card`             | `oklch(0.9820 0.0022 219.5)`  | `oklch(0.2520 0.0310 240)`    | 観測欄の地。紙よりわずかに明るい |
| `muted`            | `oklch(0.9180 0.0048 219.5)`  | `oklch(0.2980 0.0370 238)`    | 目盛りの背、無効状態             |
| `muted-foreground` | `oklch(0.5000 0.0180 241)`    | `oklch(0.7020 0.0230 230)`    | ラベル、単位、補足               |
| `border`           | `oklch(0.8601 0.0117 217.56)` | `oklch(0.3400 0.0450 233)`    | 罫線と欄の境界                   |
| `grid`             | `oklch(0.8980 0.0090 218)`    | `oklch(0.2760 0.0360 238)`    | 方眼の細線                       |
| `grid-major`       | `oklch(0.8380 0.0130 218)`    | `oklch(0.3180 0.0420 236)`    | 方眼の太線（5マスごと）          |

### Level Scale

大気質・UV・快適度・風速のように段階を持つ指標に使う。1が良好、5が危険。明るい配色では紙の上で読めるように暗く、暗い配色では青焼きの上で読めるように明るく取る。

| Token     | Light                      | Dark                       | 意味     |
| --------- | -------------------------- | -------------------------- | -------- |
| `level-1` | `oklch(0.5200 0.0900 175)` | `oklch(0.7600 0.1100 175)` | 良好     |
| `level-2` | `oklch(0.5000 0.1000 245)` | `oklch(0.7400 0.1050 245)` | 普通     |
| `level-3` | `oklch(0.5700 0.1150 75)`  | `oklch(0.8000 0.1300 80)`  | やや注意 |
| `level-4` | `oklch(0.5600 0.1600 45)`  | `oklch(0.7400 0.1550 48)`  | 注意     |
| `level-5` | `oklch(0.5000 0.1900 25)`  | `oklch(0.6800 0.1900 25)`  | 危険     |

段階色は必ず文字ラベルと組で表示する。色そのものに意味を持たせて単独で置かない。

### Chart

| Token     | Light                         | Dark                       | 系列       |
| --------- | ----------------------------- | -------------------------- | ---------- |
| `chart-1` | `oklch(0.5857 0.1841 35.50)`  | `oklch(0.7000 0.1700 38)`  | 気温       |
| `chart-2` | `oklch(0.5237 0.0860 179.14)` | `oklch(0.7200 0.1000 185)` | 湿度・降水 |
| `chart-3` | `oklch(0.4800 0.0900 250)`    | `oklch(0.7400 0.1000 245)` | PM2.5      |
| `chart-4` | `oklch(0.6799 0.1381 75.21)`  | `oklch(0.7800 0.1300 78)`  | 風速       |
| `chart-5` | `oklch(0.5200 0.1500 300)`    | `oklch(0.7200 0.1400 300)` | UV 指数    |

## 3. Typography

### Font

| Role           | Family              | 用途                               |
| -------------- | ------------------- | ---------------------------------- |
| `font-display` | Archivo             | 欧文の見出し、欄名、単位ラベル     |
| `font-sans`    | Zen Kaku Gothic New | 和文すべて                         |
| `font-mono`    | Sometype Mono       | 観測値、座標、時刻、グラフの目盛り |

Zen Kaku Gothic New は日本語の字数が多く分割配信されるため、`preload` を無効にして表示に必要な範囲だけ取得する。

数値は必ず `font-mono` で表示し、`font-variant-numeric: tabular-nums` を効かせて桁位置を固定する。値が毎秒更新される時計や、縦に並ぶグラフの目盛りが揺れないようにするため。

### Scale

Tailwind の既定サイズで足りないものだけ、役割の名前を付けて `@theme` に置く。各ファイルに `text-[3rem]` のような数値を直接書かない。

| Token              | Size        | Line height | Letter spacing | 用途                 |
| ------------------ | ----------- | ----------- | -------------- | -------------------- |
| `text-observation` | `4.5rem`    | `0.85`      | `-0.04em`      | ヒーローの気温       |
| `text-reading`     | `2.25rem`   | `1`         | `-0.02em`      | 観測欄の主数値       |
| `text-heading`     | `1.0625rem` | `1.4`       | `-0.01em`      | 欄の見出し           |
| `text-eyebrow`     | `0.6875rem` | `1.2`       | `0.14em`       | 欄名（大文字）、単位 |
| `text-note`        | `0.8125rem` | `1.5`       | `0`            | 補足、出典           |

`text-eyebrow` は `uppercase` と組で使う。欄名は観測記録紙の項目名にあたるため、本文と混ざらない字面にする。

## 4. Grid

方眼の間隔は `--spacing-grid` に持ち、余白とグラフの目盛りが同じ値を参照する。

| Token                  | Value    | 用途                      |
| ---------------------- | -------- | ------------------------- |
| `--spacing-grid`       | `1.5rem` | 方眼1マス。余白の基準     |
| `--spacing-grid-major` | `7.5rem` | 方眼の太線の間隔（5マス） |

背景の方眼は `components/ui/grid-field.tsx` が `repeating-linear-gradient` で描く。データに依存せず、テーマにだけ追従する。方眼は内容と一緒にスクロールする。画面に貼り付いた背景ではなく、内容が乗っている紙そのものとして扱うため。

グラフの横罫線は方眼の太線と同じ色を使う。ただし位置は Y 軸の目盛りに合わせる。方眼の間隔に強制すると、値と対応しない線が並び、§ 1 の「誤解を招く表現を避ける」に反するため。

## 5. Layout

```
┌ CITY OBSERVATORY ──────── 都市選択 ── 比較 ── テーマ ┐
│                                                        │
│ 東京  35.69°N 139.69°E              2026-09-17 10:36  │
│ ──────────────────────────────────────────────────────  │
│                                                        │
│  23 ℃   晴れ                        体感 25 ℃         │
│                                     湿度 73 %          │
│ ──────────────────────────────────────────────────────  │
│                                                        │
│  UV 指数 │  快適度 │  外出リスク │  風速   │  PM2.5    │
│  3.1     │  66     │  低         │  7.1    │  10.7     │
│  中程度  │  普通   │             │  北北東 │  良好     │
│ ──────────────────────────────────────────────────────  │
│                                                        │
│  日の出 05:25 ────────●──────────── 日入 17:46         │
│ ──────────────────────────────────────────────────────  │
│                                                        │
│ ┌ 気温の推移 ─────────┐  ┌ 観測地点 ────────────┐    │
│ │                      │  │                       │    │
│ └──────────────────────┘  └───────────────────────┘    │
└────────────────────────────────────────────────────────┘
```

### Container

| Breakpoint    | Padding   | Columns                        |
| ------------- | --------- | ------------------------------ |
| 〜767px       | `1.5rem`  | 1                              |
| 768px〜1023px | `2.25rem` | 2                              |
| 1024px〜      | `3rem`    | 4（観測値）/ 2（グラフと地図） |

最大幅は `90rem`。それ以上の画面では中央に寄せ、紙の幅を保つ。

### Rules

欄と欄は罫線1本で区切る。枠で囲むのは、中に別の座標系を持つもの（グラフ、地図）だけにする。観測値の欄は枠を持たず、上下の罫線だけで区切る。

## 6. Depth

影を使わない。奥行きは罫線の太さと明度差だけで表す。

| Level      | 表現                         |
| ---------- | ---------------------------- |
| 紙         | `background`                 |
| 欄         | `card` + 上下の `border` 1px |
| 枠を持つ欄 | `card` + 四辺の `border` 1px |

`backdrop-filter` と `box-shadow` は使わない。地図と MapLibre の操作部品だけが例外で、これらはライブラリ側の既定に従う。

## 7. Motion

見せ場は読み込み時の1回だけに集める。

| Animation    | Duration | Easing                       | 対象                     |
| ------------ | -------- | ---------------------------- | ------------------------ |
| `rule-draw`  | `560ms`  | `cubic-bezier(0.2, 0, 0, 1)` | 罫線が左から右へ引かれる |
| `reading-in` | `420ms`  | `cubic-bezier(0.2, 0, 0, 1)` | 観測値が4px下から現れる  |

段ごとに `80ms` ずつ遅らせ、上から順に記入されていくように見せる。

ホバーで要素を動かさない。状態の変化は罫線と文字色だけで示す。

`prefers-reduced-motion: reduce` のときは両方とも無効にする。

## 8. Map

| Theme | Style                   |
| ----- | ----------------------- |
| Light | MapTiler `dataviz`      |
| Dark  | MapTiler `dataviz-dark` |

`dataviz` はデータを重ねる前提で彩度を落とした地図で、観測地点の点より地図が目立つことがない。テーマの切り替えに追従してスタイルを差し替える。

地図は主役ではない。6都市のどこを見ているかを示す欄として、グラフと同じ大きさで並べる。

## 9. Charts

Recharts の色は CSS 変数を参照し、数値を直接書かない。

- 折れ線は `chart-1`〜`chart-5` を系列ごとに使う
- 軸のラベルは `muted-foreground`、`font-mono`
- 横罫線は `grid-major` の色。位置は Y 軸の目盛りに合わせる
- Y 軸は 0 を含む範囲で描く
- 点の強調は塗りつぶしの円のみ。影と光彩を使わない

## 10. Do's and Don'ts

### Do

- 数値は `font-mono` で桁を揃える
- 段階色には文字ラベルを併記する
- 余白は `--spacing-grid` の倍数で取る
- 欄の区切りは罫線で行う
- 明るい配色と暗い配色の両方で確認してから出す

### Don't

- `text-[3rem]` のようにサイズや字間を直接書かない
- `#87CEEB` のように色を直接書かない
- `backdrop-filter` と `box-shadow` で奥行きを作らない
- ホバーで要素を移動させない
- 1つの欄に2つ以上の観測値を詰めない
- グラフの Y 軸をゼロ以外から始めない
